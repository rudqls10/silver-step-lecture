# 흐름 문서 (Routes & Flows)

> **버전:** 1.0.0 → 1.0.1
> **작성일:** 2026-07-26 / **수정일:** 2026-08-01
> **작성자:** Hermes Agent

### 변경 이력
- **2026-07-26 v1.0.0:** 초기 작성
- **2026-08-01 v1.0.1:** 버전 관리 및 변경 이력 추가, 보안·접근성·모니터링·테스트·배포·국제화·포맷 개선 사항 반영

---

## 페이지 구조

| 경로 | 설명 |
|------|------|
| `/login` | 로고, 서비스 소개 및 Google 로그인 (방문 코치 세팅 모드 포함) |
| `/onboarding` | 로그인 직후 기초 신체 정보, 질환, 보호자 연락처 입력 (5단계) |
| `/` | 메인 운동 화면 (Zero-Touch & 100% VUI 기반 AI 전신 관절 자동 인식) |
| `/exercise/result` | 운동 성과 피드백, 시니어 감성 상장 및 보호자 리포트 전송 상태 |
| `/my` | 시니어/보호자 개인 운동 기록 및 메일 발송 이력 조회 |
| `/admin` | B2B2G 기관용 노인 낙상 위험도 관제 및 예산 ROI 리포트 대시보드 |

---

## 보호 규칙

- 로그인하지 않은 사용자는 `/login`만 접근 가능
- 온보딩을 완료하지 않은 로그인 사용자는 로그인 시 `/onboarding`으로 이동
- 온보딩을 완료한 사용자가 `/login`에 접근하면 메인 운동 화면(`/`)으로 이동
- `/my`는 로그인 필수
- `/admin`은 로그인 세션 확인 및 관리자(`admin`) role 검증 필수

---

## 정상 흐름

```
/login
  ↓
Google 로그인 (필요시 방문 코치 대행)
  ↓
/onboarding (신체, 질환, 보호자 정보 입력)
  ↓
/ (메인 운동 화면)
  ↓
바닥 안심 스티커 진입 & AI 전신 자동 인식
  ↓
Zero-Touch & 100% VUI 음성 코칭 운동 수행
  ↓
/exercise/result (운동 성과 및 감성 피드백 확인)
  ↓
Supabase 기록 저장 (관절 좌표 데이터)
  ↓
Agentria Gmail 어빌리티로 보호자 알림 자동 발송
  ↓
/my 에서 지난 운동 기록 및 메일 발송 상태 확인
```

---

## Gmail 실패 흐름

```
운동 완료 및 좌표 데이터 저장 성공
  ↓
보호자 대상 Gmail 발송 요청
  ↓
발송 실패 (네트워크/API 오류)
  ↓
운동 기록은 안전하게 DB에 유지
  ↓
email_delivery_logs에 failed 상태 저장
  ↓
결과 화면(/exercise/result) 및 /my 페이지에 '보호자에게 다시 보내기' 버튼 표시
```

---

## 카메라 권한 실패 흐름

```
카메라 접근 권한 요청
  ↓
사용자 또는 기기 설정에서 거부
  ↓
"카메라를 켜야 AI 음성 코칭을 시작할 수 있어요" 대형 안내 문구 출력
  ↓
권한 허용 재시도 안내 및 VUI 음성 가이드 제공
```

---

## 관리자 흐름 (B2B2G SaaS)

```
/admin 접근
  ↓
로그인 세션 확인
  ↓
서버에서 profiles.role = 'admin' 확인
  ↓
admin 확인 시: 1 대 다 낙상 위험도 관제 및 ROI 리포트 통계 표시
  ↓
일반 사용자: 403 Forbidden 및 메인 화면(/)으로 이동
```

---

## 로그 발생 지점 (activity_logs)

| 이벤트 | event_type |
|--------|-----------|
| 로그인 성공 | `login` |
| 로그아웃 | `logout` |
| 페이지 진입 | `page_view` |
| 온보딩 완료 | `onboarding_complete` |
| 카메라 인식 성공 | `pose_detected` |
| 운동 시작/완료 | `exercise_started`, `exercise_completed` |
| 메일 요청 | `email_send_requested` |
| 메일 성공 | `email_send_succeeded` |
| 메일 실패 | `email_send_failed` |
| 관리자 페이지 조회 | `admin_view` |
| 외부 API 실패 | `api_failed` |

---

## 에러 처리 및 재시도 정책

| 오류 유형 | 최대 재시도 | 지수 백오프 | 최종 사용자 메시지 | 로그 기록 규칙 |
|----------|------------|------------|------------------|---------------|
| Gmail 발송 실패 | 3회 | 1s → 2s → 4s | "일시적인 오류가 발생했습니다. 잠시 후 다시 시도해 주세요." | `email_send_failed` + 에러 코드, 타임스탬프, request_id |
| 네트워크 타임아웃 (HTTP 504) | 3회 | 1s → 2s → 4s | "네트워크 연결이 불안정합니다. 다시 시도해 주세요." | `api_failed` + status=504 |
| DB 연결 실패 | 3회 | 1s → 2s → 4s | "서버 연결에 문제가 발생했습니다. 잠시 후 이용해 주세요." | `db_connection_failed` |
| AI 모델 추론 실패 | 2회 | 1s → 2s | "AI 분석에 실패했습니다. 다시 시도해 주세요." | `model_inference_failed` |

---

## 보안 및 개인정보 보호 (GDPR/PIPA)

**인증/인가**
- 액세스 토큰: HttpOnly, Secure 플래그 설정된 쿠키에 저장
- 리프레시 토큰: 동일한 방식으로 저장, rotation 적용
- 비밀번호 정책: 최소 8자, 대소문자·숫자·특수문자 포함, 90일 만료

**데이터 보호**
- 건강 데이터(관절 좌표, 운동 기록) 및 보호자 연락처는 AES-256 암호화로 DB에 저장
- 전송 시 TLS 1.2 이상 필수 적용

**로그 관리**
- 로그에 개인 식별 정보(PII) 노출 금지 → 이메일, 전화번호, 건강 지표 등은 해시 또는 마스킹 처리
- 접근 로그는 별도 보안 영역에 6개월 보관 후 파기

---

## 접근성 (Accessibility)

**음성 UI (VUI)**
- 음성 프롬프트는 간결하게 (최대 2문장) 및 명확한 발음 위주 작성
- 오류 발생 시 음성 재시도 안내: "잠시 후 다시 말씀해 주세요"

**시각 보조**
- 모든 이미지·아이콘에 의미 있는 alt 텍스트 또는 ARIA 라벨 제공
- 고대비 모드 지원 (WCAG AA 대비 비율 충족)

**키보드 navigability**
- 모든 대화형 요소는 탭 순서로 접근 가능하도록 tabindex 관리

---

## 모니터링 및 알림

**핵심 지표**
- 로그인 성공률, 온보딩 완료율, 운동 완료율, 이메일 발송 성공률

**임계치 기반 알림 (운영팀에 Slack/Email 전송)**
- 로그인 성공률 95% 미만 → 경고
- 이메일 발송 실패율 5% 초과 → critical

**대시보드 예시 항목**
- 시간별 로그인 시도 수
- 카메라 권한 거부 비율
- 평균 운동 세션 길이

---

## 테스트 및 품질 보증

- **단위 테스트:** 로그인/로그아웃, 온보딩 단계별 입력 검증 / 카메라 권한 거부 시 UI 텍스트 노출 확인
- **통합 테스트:** 운동 시작 → 결과 저장 → 이메일 발송 흐름 전체 시나리오
- **UI 자동화 테스트 (예: Playwright, Cypress):** 주요 흐름 각각에 대한 스냅샷 테스트
- **성능 테스트:** 동시 1,000 사용자 시뮬레이션 하에서 응답 시간 2초 이내 보장

---

## 배포 및 롤백 전략

- **배포 방식:** 블루/그린 배포 또는 카나리 배포 (신규 버전 5% 트래픽부터 시작)
- **롤백 절차:**
  - 배포 후 15분 내 주요 지표(오류율, latency) 임계치 초과 시 자동 롤백 트리거
  - 데이터베이스 스키마 변경 시점에는 마이그레이션 스크립트와 함께 롤백 스크립트 준비
- **배포 알림:** 배포 시작·종료 시 Slack #deploy-channels에 자동 메시지 전송

---

## 주석 및 예시 보강

- 각 흐름 블록 하단에 실제 화면 예시 링크 또는 스크린샷 경로를 주석 형태로 추가

```
/login: 로고, 서비스 소개 및 Google 로그인 (방문 코치 세팅 모드 포함)
참고 화면: ./wireframes/login.png
```

- 주요 결정 rationale (예: 왜 지수 백오프를 선택했는지)도 간단히 주석 처리

---

## 국제화 (i18n) 고려

- 모든 사용자 표시 문자열은 외부 JSON(i18n) 파일로 분리
  - `locales/ko.json`, `locales/en.json` 등
- 초기 배포는 한국어(ko) 우선, 향후 영어(en) 등 추가 시 키 기반 교체로 진행
