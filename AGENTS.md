# 교육용 AI 웹앱 공통 개발 규칙

이 파일은 프로젝트의 최상위 작업 규칙이다.
앱의 주제와 기능은 PROJECT_BRIEF.md에 기록한다.
API 요청과 응답은 API_CONTRACT.md에 기록한다.
데이터 구조는 DATA_MODEL.md에 기록한다.
화면과 이동 흐름은 ROUTES_AND_FLOWS.md에 기록한다.
참고 이미지는 UI_REFERENCE.md에 기록한다.

이 파일은 임의로 수정하거나 삭제하지 않는다.

---

## 1. 작업 전 필수 확인

어떤 파일도 수정하기 전에 다음 순서로 읽는다.

1. AGENTS.md
2. PROJECT_BRIEF.md
3. ROUTES_AND_FLOWS.md
4. UI_REFERENCE.md
5. API_CONTRACT.md
6. DATA_MODEL.md
7. ENVIRONMENT.md
8. package.json
9. 현재 프로젝트 파일 구조

문서 내용이 비어 있거나 서로 충돌하면 추측해서 구현하지 않는다.
먼저 충돌 지점과 필요한 질문을 사용자에게 보고한다.

---

## 2. 작업 순서

모든 작업은 다음 순서를 지킨다.

1. 관련 문서와 코드 읽기
2. 현재 동작 확인
3. 수정 계획 보고
4. 한 번에 한 가지 작업만 수행
5. 필요한 파일만 최소 수정
6. 개발 서버에서 직접 확인
7. 빌드 명령 실행
8. 오류가 있으면 원인을 확인한 뒤 수정
9. 수정 파일과 테스트 결과 보고

사용자가 명시적으로 구현을 요청하기 전에는 코드를 수정하지 않는다.
분석 요청에는 분석만 답한다.
계획 요청에는 계획만 답한다.

---

## 3. 절대 금지 사항

- 새 프로젝트를 만들지 않는다.
- 현재 프레임워크를 다른 프레임워크로 변경하지 않는다.
- 기존 프로젝트 전체를 다시 작성하지 않는다.
- 요청받지 않은 페이지나 기능을 추가하지 않는다.
- 기존 페이지 경로를 임의로 변경하지 않는다.
- 기존 디자인을 이유 없이 전면 교체하지 않는다.
- 새로운 UI 프레임워크를 임의로 설치하지 않는다.
- 패키지를 설치하기 전에 필요성과 대안을 먼저 설명한다.
- 로그인, 결제, 데이터베이스, 관리자 기능을 임의로 제거하지 않는다.
- 작동 중인 API 코드를 UI 수정 과정에서 삭제하지 않는다.
- API 키, service role key, 비밀 토큰을 클라이언트 코드에 작성하지 않는다.
- `.env`, `.env.local`을 Git에 커밋하지 않는다.
- `SUPABASE_SERVICE_ROLE_KEY`를 브라우저 코드에서 사용하지 않는다.
- 확인하지 않은 API 주소나 응답 구조를 만들어내지 않는다.
- 테스트 없이 완료했다고 보고하지 않는다.

---

## 4. 공통 앱 구조

모든 프로젝트는 다음 기능을 유지한다.

- Google 로그인
- 로그인 사용자 식별
- 사용자별 데이터 저장
- 사용자별 활동 로그 저장
- LLM 또는 외부 API 처리
- Gmail 발송 기능
- 관리자 전용 페이지
- 일반 사용자와 관리자 권한 분리
- 로딩, 성공, 실패 상태
- GitHub 코드 관리
- Vercel 배포 가능 구조

앱별 핵심 기능은 PROJECT_BRIEF.md에서 정의한다.

---

## 5. Google 로그인 규칙

- Google 로그인은 Supabase Auth를 기준으로 구현한다.
- 로그인 후 Supabase 사용자 ID를 앱의 기본 사용자 식별자로 사용한다.
- 이메일 주소를 기본키로 사용하지 않는다.
- 로그인에서 받은 이름, 이메일, 프로필 이미지는 profiles 테이블에 필요한 범위만 저장한다.
- 로그인 성공과 로그아웃은 활동 로그에 기록한다.
- OAuth 토큰을 일반 데이터 테이블에 저장하지 않는다.
- 브라우저에 provider access token을 노출하거나 로그로 출력하지 않는다.

---

## 6. Supabase 규칙

- 모든 사용자 데이터 테이블에 user_id를 포함한다.
- 일반 사용자는 자기 데이터만 읽고 수정할 수 있어야 한다.
- 사용자별 데이터 분리는 RLS 정책으로 구현한다.
- 관리자 권한은 UI 숨김만으로 처리하지 않는다.
- 관리자 API와 관리자 페이지는 서버에서 권한을 다시 확인한다.
- 클라이언트에서 service role key를 사용하지 않는다.
- 큰 원문, 불필요한 HTML, 긴 이메일 본문을 로그 테이블에 저장하지 않는다.
- 로그의 metadata는 필요한 짧은 값만 저장한다.
- DB가 없다면 더미데이터를 사용한다.
- vercel 배포하지 않고, 로컬에서 실행할 것이다.

---

## 7. 관리자 규칙

- 관리자 페이지는 `/admin` 경로를 기본으로 한다.
- 관리자 여부는 profiles.role 또는 별도 관리자 테이블에서 확인한다.
- 일반 사용자가 `/admin`에 직접 접근하면 접근을 거부한다.
- 관리자 조회는 서버 측 권한 검사를 통과한 후 수행한다.
- 관리자 화면에서 비밀키, OAuth 토큰, 이메일 본문을 표시하지 않는다.
- 개인정보는 수업 목적에 필요한 최소 항목만 보여준다.

---

## 8. 외부 API 규칙

- API_CONTRACT.md에 확인된 요청만 구현한다.
- Postman 또는 공식 문서에서 성공하지 않은 요청을 추측하지 않는다.
- 비밀 API 키가 필요한 요청은 서버 Route 또는 서버 함수에서 실행한다.
- 브라우저용 공개 키는 도메인과 API 제한을 적용한다.
- 외부 API 응답 전체를 데이터베이스에 저장하지 않는다.
- 화면과 기능에 필요한 필드만 정규화하여 사용한다.
- 타임아웃, 401, 403, 404, 429, 500 오류를 처리한다.

---

## 9. Gmail 및 Agentria 규칙

- Gmail 발송은 Agentria에서 만든 Gmail 어빌리티 API를 통해 요청한다.
- 브라우저에서 Agentria 비밀키를 직접 호출하지 않는다.
- 앱 내부 서버 Route가 Agentria API를 호출한다.
- 메일 본문과 제목 전체를 데이터베이스에 저장하지 않는다.
- 이메일 발송 로그에는 다음만 저장한다.
  - user_id
  - 관련 데이터 ID
  - 발송 상태
  - HTTP 상태 코드
  - Agentria 요청 ID 또는 메시지 ID가 있을 경우 해당 ID
  - 짧은 오류 코드
  - 발송 시각
- 발송 실패 시 사용자 데이터와 별점 데이터는 유지한다.
- 메일 발송 실패 때문에 핵심 기능 전체를 실패 처리하지 않는다.

---

## 10. LLM 규칙

- LLM의 역할은 PROJECT_BRIEF.md에 정의된 범위로 제한한다.
- 장소 검색, 사용자 인증, 권한 확인을 LLM에게 맡기지 않는다.
- LLM 입력에 API 키, 토큰, 불필요한 개인정보를 포함하지 않는다.
- LLM 결과를 그대로 신뢰하지 않고 화면 출력용으로 검증한다.
- 긴 원문 전체를 데이터베이스에 저장하지 않는다.
- 필요하면 요약 결과나 짧은 추천 이유만 저장한다.

---

## 11. 참고 이미지 규칙

참고 이미지는 디자인 방향을 설명하는 자료다.

참고할 수 있는 요소:
- 레이아웃
- 여백
- 글자 크기 위계
- 카드 형태
- 버튼 위치
- 색상 분위기
- 정보의 우선순위

복사하지 않을 요소:
- 타사 로고
- 브랜드명
- 상표
- 이미지 속 개인정보
- 저작권이 있는 사진 원본
- 이미지에 보인 불필요한 기능

이미지를 보고 바로 수정하지 않는다.
먼저 다음을 보고한다.

1. 이미지에서 확인한 특징
2. 현재 화면과 다른 점
3. 유지할 기존 기능
4. 수정할 파일
5. 예상 결과

한 번에 한 화면만 수정한다.

---

## 12. UI 수정 시 유지해야 할 기능

- 로그인과 로그아웃
- 사용자 정보 표시
- 입력값 상태
- 버튼 클릭
- 페이지 이동
- 로딩 상태
- API 호출
- 결과 표시
- 별점 저장
- Gmail 발송
- 오류 표시
- 관리자 권한 확인

---

## 13. 로그 규칙

활동 로그는 분석에 필요한 최소 정보만 저장한다.

허용 예시:
- login
- logout
- page_view
- place_search
- place_selected
- rating_created
- email_send_requested
- email_send_succeeded
- email_send_failed
- admin_view
- api_failed

금지 예시:
- 전체 이메일 본문
- 전체 LLM 프롬프트
- 전체 API 원문 응답
- OAuth 토큰
- 비밀키
- 사용자가 입력한 민감정보 전체

---

## 14. 작업 단위

좋은 작업 예:
- 로그인 화면만 구현
- 현재 위치 권한 처리만 구현
- 주변 장소 검색 API Route만 구현
- 별점 저장만 구현
- Gmail 발송 Route만 구현
- 관리자 권한 검사만 구현

나쁜 작업 예:
- 앱 전체를 한 번에 완성
- 디자인, 로그인, DB, API, Gmail, 관리자, 배포를 한 번에 처리

---

## 15. 완료 조건

- 기존 페이지가 사라지지 않았다.
- 로그인과 로그아웃이 작동한다.
- 일반 사용자는 자기 데이터만 볼 수 있다.
- 관리자만 관리자 페이지를 볼 수 있다.
- 입력, 로딩, 성공, 실패 상태가 있다.
- API 키가 브라우저와 GitHub에 노출되지 않는다.
- Gmail 본문이 DB에 저장되지 않는다.
- 모바일에서 가로 스크롤이 발생하지 않는다.
- 개발 서버가 실행된다.
- `npm run build`가 성공한다.

---

## 16. 작업 완료 보고 형식

### 수행한 작업
- ...

### 수정한 파일
- ...

### 유지한 기존 기능
- ...

### 데이터베이스 변경
- 없음 또는 변경 내용

### 보안 확인
- 클라이언트 비밀키 노출 여부:
- RLS 확인:
- 관리자 권한 확인:

### 테스트 결과
- 개발 서버:
- 빌드:
- 로그인:
- 핵심 기능:
- Gmail:
- 관리자 페이지:

### 남은 문제
- 없음 또는 구체적인 문제
